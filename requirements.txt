# RSS feed parsing
feedparser>=6.0.10

# Video generation
moviepy>=1.0.3
pillow>=10.0.0

# Audio processing
pydub>=0.25.1
# Python 3.13 removed stdlib 'audioop'. Use a maintained backport on 3.13+.
# `audioop-lts` provides a drop-in `audioop` module compatible with pydub.
audioop-lts>=0.2.1; python_version >= "3.13"

# Utilities
numpy>=1.24.0
pyyaml>=6.0
